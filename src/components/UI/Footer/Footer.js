import React from "react";
import classes from './Footer.css';

const footer = (props) => (
    <div className={classes.Footer}>
      <p className={classes.Text}>Burger Bazaar: One stop shop for all your needs</p>  
    </div>
);

export default footer;